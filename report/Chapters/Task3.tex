%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%	TASK 3 - Evaluation Methodology and Analysis
%   Maps to brief Tasks 3.1, 3.2, 3.3.
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\lhead{Task 3. \emph{Evaluation Methodology and Analysis}}
\section{Task 3: Evaluation Methodology and Analysis}
\label{Task3}

\subsection{Parameter Sensitivity Analysis (Task 3.1)}
\label{T3:Sensitivity}

A full-factorial parameter sweep was conducted on kroA100 using PMX crossover
with repair enabled. The swept parameters and their levels are:
population size $\in \{50, 100, 200\}$,
crossover rate $\in \{0.70, 0.85, 0.95\}$,
mutation rate $\in \{0.01, 0.05, 0.10\}$,
and selection method $\in \{\text{tournament}, \text{roulette}\}$.
This yields $3 \times 3 \times 3 \times 2 = 54$ unique configurations, each
executed for 30 independent seeds (1,620 total runs of 500 generations each).

\tref{tab:sweep-top} reports the five best and five worst configurations
ranked by mean best fitness over 30 seeds.

\begin{table}[h]
    \centering
    \caption{Top-5 and bottom-5 configurations from the parameter sweep, ranked by mean best fitness (lower is better). Gap is relative to the known optimal tour length of 21,282.}
    \label{tab:sweep-top}
    \begin{tabular}{r r r l r r r}
        \hline
        Pop.\ size & $r_c$ & $r_m$ & Selection & Mean best & Std. & Gap \\
        \hline
        200 & 0.95 & 0.10 & tournament & 47,539 & 3,455 & 123.4\% \\
        200 & 0.85 & 0.10 & tournament & 48,649 & 3,718 & 128.6\% \\
        200 & 0.70 & 0.10 & tournament & 49,179 & 3,342 & 131.1\% \\
        200 & 0.95 & 0.05 & tournament & 52,959 & 3,672 & 148.8\% \\
        200 & 0.85 & 0.05 & tournament & 53,617 & 3,310 & 151.9\% \\
        \hline
        200 & 0.85 & 0.05 & roulette & 101,085 & 5,998 & 375.0\% \\
        200 & 0.85 & 0.01 & roulette & 101,393 & 6,604 & 376.4\% \\
        200 & 0.95 & 0.01 & roulette & 104,816 & 5,335 & 392.5\% \\
        200 & 0.95 & 0.10 & roulette & 105,085 & 5,718 & 393.8\% \\
        200 & 0.95 & 0.05 & roulette & 106,911 & 4,928 & 402.4\% \\
        \hline
    \end{tabular}
\end{table}

Three patterns emerge from \tref{tab:sweep-top} and the per-parameter
boxplots in \fref{fig:boxplot-pop}--\fref{fig:boxplot-selection}.

\textbf{Selection method} is the dominant factor.
Tournament selection (median 63,674) substantially outperforms roulette
wheel selection (median 89,059). All five best configurations use
tournament, and all five worst use roulette. Tournament's deterministic
best-of-$k$ rule creates stronger selection pressure than roulette's
proportional sampling, driving faster convergence toward shorter tours
on this problem instance.

\textbf{Mutation rate} is the second-largest effect.
Among the three levels tested, $r_m = 0.10$ produces the lowest median
(71,606), followed by $r_m = 0.05$ (76,747) and $r_m = 0.01$ (90,322).
The lowest rate likely causes premature convergence because swap mutation
at $r_m = 0.01$ perturbs the population too infrequently to escape local
optima within 500 generations.

\textbf{Population size} has a consistent but comparatively small effect.
Larger populations produce lower median tour lengths (200~$\to$~78,366;
100~$\to$~80,377; 50~$\to$~81,744), but the gap between the three levels
is modest compared to the within-group variance. In
\tref{tab:sweep-top}, all five best configurations use pop\_size~$=$~200,
suggesting that the additional diversity from larger populations
compounds with other favourable settings.

\begin{figure}[h]
    \centering
    \includegraphics[width=0.75\textwidth]{../results/figures/boxplot_pop_size.pdf}
    \caption{Distribution of final tour length by population size (PMX, repair enabled, 30 seeds per configuration).}
    \label{fig:boxplot-pop}
\end{figure}

\begin{figure}[h]
    \centering
    \includegraphics[width=0.75\textwidth]{../results/figures/boxplot_mutation_rate.pdf}
    \caption{Distribution of final tour length by mutation rate.}
    \label{fig:boxplot-mutation}
\end{figure}

\begin{figure}[h]
    \centering
    \includegraphics[width=0.75\textwidth]{../results/figures/boxplot_selection_method.pdf}
    \caption{Distribution of final tour length by selection method.}
    \label{fig:boxplot-selection}
\end{figure}

Crossover rate ($r_c$) has the smallest effect among the four parameters.
The convergence curves in \fref{fig:conv-pop}--\fref{fig:conv-selection}
further confirm that tournament selection and higher mutation rates
produce steeper initial convergence and lower final plateaus, while
population size shifts the entire curve downward without changing its
shape.

\subsection{Performance Evaluation (Task 3.2)}
\label{T3:Performance}

The best configuration identified in the sweep
(pop\_size~$=$~200, $r_c$~$=$~0.95, $r_m$~$=$~0.10, tournament selection)
achieved a mean best fitness of 47,539 with a standard deviation of
3,455 across 30 seeds. The best single run reached 37,956, a gap of
78.4\% from the known optimal of 21,282. The worst configuration
(pop\_size~$=$~200, $r_c$~$=$~0.95, $r_m$~$=$~0.05, roulette) reached a
mean of 106,911, a gap of 402.4\%.

\begin{figure}[h]
    \centering
    \includegraphics[width=0.75\textwidth]{../results/figures/convergence_best_vs_worst.pdf}
    \caption{Convergence comparison between the best and worst configurations from the parameter sweep.}
    \label{fig:conv-best-worst}
\end{figure}

\fref{fig:conv-best-worst} shows the convergence curves for the best and
worst configurations. The best configuration converges steeply for the
first 100 generations and continues improving at a slower rate until
generation 500. The worst configuration shows minimal improvement after
the initial generations, consistent with weak selection pressure from
roulette combined with insufficient mutation to escape local optima.

\textbf{Convergence rate.} \fref{fig:conv-pop}--\fref{fig:conv-selection}
show convergence curves grouped by each parameter. Tournament selection
configurations reach lower fitness values by generation 100 than roulette
reaches by generation 500. Higher mutation rates produce steeper descent
in the first 50 generations. Larger populations show a modest downward
shift across all generations.

\begin{figure}[h]
    \centering
    \includegraphics[width=0.75\textwidth]{../results/figures/convergence_by_pop_size.pdf}
    \caption{Convergence by population size (mean $\pm$ 1 std over 30 seeds).}
    \label{fig:conv-pop}
\end{figure}

\begin{figure}[h]
    \centering
    \includegraphics[width=0.75\textwidth]{../results/figures/convergence_by_mutation_rate.pdf}
    \caption{Convergence by mutation rate.}
    \label{fig:conv-mutation}
\end{figure}

\begin{figure}[h]
    \centering
    \includegraphics[width=0.75\textwidth]{../results/figures/convergence_by_selection.pdf}
    \caption{Convergence by selection method.}
    \label{fig:conv-selection}
\end{figure}

\textbf{Stability across trials.} The standard deviation in \tref{tab:sweep-top}
ranges from 3,310 to 6,604. Tournament configurations are more consistent
(std $\approx$ 3,300--3,700) than roulette configurations (std $\approx$
4,900--6,600), indicating that stronger selection pressure reduces
run-to-run variability.

\textbf{Computational efficiency.} All runs used a fixed budget of 500
generations. The wall-clock time per run scales linearly with population
size (approximately 0.2s for pop\_size~$=$~50, 0.4s for 100, and 0.8s for
200 on the test machine). The improvement from pop\_size~$=$~100 to 200 is
small relative to the doubling in runtime, suggesting that pop\_size~$=$~100
or 200 with tournament selection offers the best quality-cost trade-off for
this instance.

\subsection{Critical Analysis of Repair Mechanism (Task 3.3)}
\label{T3:RepairCritique}
This section evaluates the repair mechanism using the controlled comparison
from TICKET-17. Both experimental arms used the same GA settings on kroA100:
population size 100, 500 generations, tournament selection, naive single-point
crossover, swap mutation, and 30 independent seeds. The only experimental
difference was whether duplicate/missing-city repair was applied after
crossover and mutation.

\begin{table}[h]
    \centering
    \caption{Sanity check showing that naive crossover creates infeasibility and that repair restores a valid permutation.}
    \label{tab:repair-sanity}
    \begin{tabular}{l r}
        \hline
        Observation from TICKET-17 sanity check & Value \\
        \hline
        Parent A valid permutation & True \\
        Parent B valid permutation & True \\
        Naive-crossover child valid permutation & False \\
        Duplicate positions in child & 23 \\
        Missing cities in child & 23 \\
        Repaired child valid permutation & True \\
        \hline
    \end{tabular}
\end{table}

\begin{table}[h]
    \centering
    \caption{Final-generation summary over 30 paired runs per strategy. Lower best fitness is numerically smaller, but the no-repair arm is infeasible under the TSP constraint.}
    \label{tab:repair-summary}
    \begin{tabular}{l r r r r r}
        \hline
        Strategy & Mean best & Std. & Min. best & Gap to optimum & Final diversity \\
        \hline
        No repair & 38129.27 & 4135.41 & 28805.71 & 79.2\% & 0.0100 \\
        Repair & 49777.84 & 3186.86 & 43890.51 & 133.9\% & 0.0128 \\
        \hline
    \end{tabular}
\end{table}

\begin{table}[h]
    \centering
    \caption{Mean best-fitness and diversity checkpoints from TICKET-17 convergence logs. Values are averaged over 30 seeds per strategy.}
    \label{tab:repair-convergence}
    \begin{tabular}{l r r r}
        \hline
        Strategy & Generation & Mean best fitness & Mean diversity \\
        \hline
        No repair & 0 & 151041.61 & 0.9900 \\
        No repair & 100 & 61144.48 & 0.0360 \\
        No repair & 250 & 43844.78 & 0.0165 \\
        No repair & 500 & 38129.27 & 0.0100 \\
        Repair & 0 & 151041.61 & 0.9900 \\
        Repair & 100 & 77552.75 & 0.0413 \\
        Repair & 250 & 59334.24 & 0.0247 \\
        Repair & 500 & 49777.84 & 0.0128 \\
        \hline
    \end{tabular}
\end{table}

\begin{table}[h]
    \centering
    \caption{Paired Wilcoxon signed-rank test on final best fitness from the 30 matched seeds in TICKET-17.}
    \label{tab:repair-wilcoxon}
    \begin{tabular}{l r}
        \hline
        Test quantity & Value \\
        \hline
        Statistic & 0.00 \\
        $p$-value & $1.86 \times 10^{-9}$ \\
        Significance level & 0.05 \\
        Decision & Reject equal medians \\
        Mean paired difference, repair $-$ no repair & 11648.57 \\
        \hline
    \end{tabular}
\end{table}

The repair mechanism is effective at enforcing feasibility in the scenario for
which it was designed. The TICKET-17 sanity check shows that naive crossover
changed two valid parents into an invalid child with 23 duplicate positions and
23 missing cities, while the repair step converted that same child back into a
valid permutation (\tref{tab:repair-sanity}). This establishes that the repair
operator is not cosmetic in this experiment: without it, the GA evaluates
chromosomes that do not represent Hamiltonian tours (\tref{tab:repair-sanity}).

The final-fitness results must be interpreted through feasibility, not raw
numeric distance alone. The no-repair arm obtained a lower mean best fitness
than the repair arm, 38129.27 versus 49777.84 (\tref{tab:repair-summary}), and
the paired Wilcoxon signed-rank test reported a test statistic of 0.00 with
$p = 1.86 \times 10^{-9}$ (\tref{tab:repair-wilcoxon}). This does not show that
no-repair is a better TSP solver. Instead, it confirms the infeasibility
pathology: when duplicate cities are allowed, the chromosome can skip some
cities and still receive a short distance from the fitness function
(\tref{tab:repair-sanity}, \tref{tab:repair-summary}). The repair arm reports
higher distances because it evaluates genuine 100-city tours after constraint
restoration (\tref{tab:repair-summary}).

The main trade-off is between strict feasibility and exploratory freedom. Repair
removes invalid chromosomes before evaluation, which protects selection pressure
from artificially short infeasible tours (\tref{tab:repair-summary}). However,
repair also edits offspring after crossover and mutation, so the relevant
empirical question is whether that editing caused faster diversity collapse in
the observed runs. In the TICKET-17 logs, both strategies lost most of their
initial Hamming diversity by generation 500, but the repair arm retained
slightly higher final diversity than no-repair (0.0128 versus 0.0100) and
stayed higher at the generation-100 and generation-250 checkpoints
(\tref{tab:repair-convergence}). This suggests that, for the random-insertion
repair used here, feasibility enforcement did not cause faster diversity
collapse than the no-repair baseline in this run (\tref{tab:repair-convergence}).

Compared with alternative constraint-handling strategies, repair is most useful
when the chosen operators can create invalid permutations. A feasibility-
preserving crossover such as PMX, OX, or CX avoids this specific duplicate/missing
city failure by construction, so the repair step would usually become a safeguard
rather than the main constraint handler. By contrast, the no-repair baseline in
TICKET-17 is equivalent to using no effective penalty for infeasibility: invalid
chromosomes survive and are selected because their raw distances are shorter
(\tref{tab:repair-summary}). A penalty-function alternative would need to assign
enough cost to duplicate and missing cities to prevent this selection-pressure
distortion; otherwise it would reproduce the same misleading advantage observed
in the no-repair arm (\tref{tab:repair-sanity}, \tref{tab:repair-summary}).

Overall, TICKET-17 supports the repair mechanism as a necessary constraint-
handling component for naive crossover experiments. The statistical test shows
that the two strategies behave differently, the sanity check shows why the
difference occurs, and the convergence checkpoints show that repair preserves
validity while maintaining slightly more final diversity than the invalid
baseline in this experiment (\tref{tab:repair-sanity},
\tref{tab:repair-summary}, \tref{tab:repair-convergence},
\tref{tab:repair-wilcoxon}). The repair arm is therefore the meaningful basis
for TSP performance discussion, while the no-repair arm is best interpreted as
evidence of how infeasibility corrupts fitness-based selection.
