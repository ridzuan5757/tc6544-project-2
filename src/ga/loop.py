"""Main GA loop wiring selection, crossover, mutation, and repair (TICKET-10).

Returns a run record (per-generation best/mean fitness, diversity, final tour).
"""


def run_ga(config, dist_matrix, rng):
    raise NotImplementedError
